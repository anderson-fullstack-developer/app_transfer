packages:
  - 'apps/*'
  - 'packages/*'

# Dependencias autorizadas a correr scripts de build/postinstall.
# Necessario para os binarios nativos do esbuild e do motor do Prisma.
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  '@scarf/scarf': false
  esbuild: true
  prisma: true
